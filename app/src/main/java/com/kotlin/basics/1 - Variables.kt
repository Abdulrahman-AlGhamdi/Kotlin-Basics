package com.kotlin.basics

fun main() {

    // val is immutable variable (cannot be reassigned)
    val name = "Abdulrahman"
    println(name)

    // var is mutable variable (can be reassigned)
    var age = 20
    println(age)

    // To change a var value, refer to the variable name
    age = 25
    println(age)

}